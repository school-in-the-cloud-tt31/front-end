const initialState = {
  tasks: [],
  isLoading: false,
  error: "",
  volunteers: [],
};

export const schoolReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
